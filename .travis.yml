
before_script:
    - git config --global user.email "testing_git@example.com"
    - git config --global user.name "Testing Releaseable Git"

script:
    - echo $(bash --version)
    - test/bin/run_all
